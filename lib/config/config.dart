class Config {
  // Setting the API url and parameters as provided by website
  static String apiUrl = 'reqres.in'; // '606cc5e5603ded00175029ee.mockapi.io';
  static String root = 'https';
  static int userId = 1;
  static String model = 'user';
  // Formatting the final URL, So we can use it directly on our app
  // static String apiUrl = '$url/api';
}
